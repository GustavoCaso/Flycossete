.container
  .admin_users
    %h1 Usuarios

    %table.table
      %tr
        %th Username
        %th Email
        %th Password
        %th
        %th
        %th

      - @users.each do |user|
        %tr
          %td= user.username
          %td= user.email
          %td= user.password
          %td= link_to 'Show', [:admin, user]
          %td= link_to 'Edit', edit_admin_user_path(user)
          %td= link_to 'Destroy', [:admin, user], :method => :delete, :data => { :confirm => '¿ Estas seguro de querer borrarlo ?' }

    %br

    = link_to 'Nuevo Usuario', new_admin_user_path, class: "admin_link"
